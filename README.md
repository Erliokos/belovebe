# BELOVEBE - Telegram Mini App (Приложение для знакомств)

Полнофункциональное Telegram Mini App для знакомств.

> 📖 **Для деплоя на production сервер см. [DEPLOY.md](./DEPLOY.md)**

## 🚀 Технологический стек

### Frontend
- React 18 + TypeScript
- Vite
- Styled Components
- Zustand (State Management)
- TanStack React Query
- React Router DOM
- @twa-dev/sdk (Telegram Web App SDK)

### Backend
- Node.js + Express.js + TypeScript
- PostgreSQL
- Prisma (ORM)
- JWT (Аутентификация)
- Swagger (API Документация)

## 📋 Предварительные требования

- Node.js 18+
- PostgreSQL 14+
- npm или yarn

## 🛠️ Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей корневого проекта
npm install

# Установка зависимостей backend
cd backend
npm install

# Установка зависимостей frontend
cd ../frontend
npm install
```

### 2. Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE belovebe;
```

2. Настройте переменные окружения в `backend/.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/belovebe?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
PORT=4000
```

3. Запустите миграции Prisma и заполните начальные данные:
```bash
cd backend
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed
```

### 3. Настройка Frontend

Создайте файл `frontend/.env` (опционально):
```env
VITE_API_URL=http://localhost:4000
```

## 🚀 Запуск проекта

### С помощью Docker (Рекомендуется)

#### Production режим

```bash
# Сборка и запуск всех сервисов
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Остановка с удалением volumes
docker-compose down -v
```

#### Development режим

```bash
# Запуск в режиме разработки
docker-compose -f docker-compose.dev.yml up -d --build

# Просмотр логов
docker-compose -f docker-compose.dev.yml logs -f

# Остановка
docker-compose -f docker-compose.dev.yml down
```

После первого запуска нужно выполнить seed (миграции выполняются автоматически):
```bash
# Для production
docker-compose exec backend npm run prisma:seed

# Для development
docker-compose -f docker-compose.dev.yml exec backend npm run prisma:seed
```

**Примечание:** В production режиме миграции выполняются автоматически при первом запуске контейнера через entrypoint скрипт.

### Без Docker (Локальная разработка)

Из корневой директории:
```bash
npm run dev
```

Или отдельно:

**Backend:**
```bash
cd backend
npm run dev
```

**Frontend:**
```bash
cd frontend
npm run dev
```

### Production сборка

```bash
npm run build
```

## 📚 API Документация

После запуска backend, Swagger документация доступна по адресу:
```
http://localhost:4000/api-docs
```

## 🐳 Docker

Проект полностью настроен для работы с Docker:

- **docker-compose.yml** - Production конфигурация
- **docker-compose.dev.yml** - Development конфигурация с hot reload
- **backend/Dockerfile** - Production образ backend
- **backend/Dockerfile.dev** - Development образ backend
- **frontend/Dockerfile** - Production образ frontend (с nginx)
- **frontend/Dockerfile.dev** - Development образ frontend

### Переменные окружения для Docker

Создайте файл `.env` в корне проекта (опционально):
```env
JWT_SECRET=your-super-secret-jwt-key-change-in-production
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
VITE_API_URL=http://localhost:4000
FRONTEND_PORT=3002
```

Если файл не создан, будут использованы значения по умолчанию из docker-compose.yml.

## 🗂️ Структура проекта

```
belovebe/
├── backend/
│   ├── src/
│   │   ├── routes/          # API роуты
│   │   ├── middleware/      # Middleware (auth)
│   │   ├── utils/           # Утилиты (JWT, Telegram)
│   │   └── index.ts        # Точка входа
│   ├── prisma/
│   │   └── schema.prisma   # Схема базы данных
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── api/            # API клиент
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы приложения
│   │   ├── stores/         # Zustand stores
│   │   ├── types/          # TypeScript типы
│   │   └── App.tsx         # Главный компонент
│   └── package.json
└── package.json
```

## 🔐 Аутентификация

Приложение использует аутентификацию через Telegram Web App:
1. Frontend получает `initData` от Telegram
2. Отправляет на `/api/auth/telegram`
3. Backend валидирует подпись и возвращает JWT токен
4. Токен хранится в памяти и добавляется к каждому запросу

## 📱 Основные функции

- **Лента заданий** - просмотр и фильтрация заданий по категориям
- **Детальная страница задания** - просмотр деталей и создание отклика
- **Мои задания** - управление своими заданиями и просмотр откликов
- **Мои отклики** - просмотр статусов своих откликов
- **Профиль** - управление профилем и шаблонами ответов
- **Шаблоны ответов** - сохранение и использование шаблонов для быстрых откликов

## 🔧 Дополнительные команды

### Prisma

```bash
# Генерация Prisma Client
npm run prisma:generate

# Создание новой миграции
npm run prisma:migrate

# Открыть Prisma Studio
npm run prisma:studio
```

## 📝 Примечания

- Токен JWT хранится в памяти, не в localStorage
- Все API endpoints (кроме `/api/auth/telegram`) требуют JWT токен
- Шаблоны ответов хранятся в localStorage
- Для работы в Telegram необходимо настроить бота и указать правильный `TELEGRAM_BOT_TOKEN`

## 🐛 Решение проблем

### Ошибка подключения к базе данных
- Проверьте, что PostgreSQL запущен
- Убедитесь, что `DATABASE_URL` в `.env` правильный
- Проверьте права доступа пользователя БД

### Ошибка валидации Telegram initData
- Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что приложение запущено через Telegram Web App

### Проблемы с CORS
- Убедитесь, что backend запущен на правильном порту
- Проверьте настройки CORS в `backend/src/index.ts`

